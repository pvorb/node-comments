#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../marked/bin/marked" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../marked/bin/marked" "$@"
  ret=$?
fi
exit $ret
